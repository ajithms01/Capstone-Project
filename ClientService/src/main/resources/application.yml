server:
  port: 8082

spring:
  application:
    name: clientservice
  datasource:
    url: jdbc:postgresql://localhost:5432/event
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
